# nginx.conf — reverse proxy for Auralis
# Place behind Certbot / Let's Encrypt for HTTPS.
# For HTTPS: replace port 80 server with 443 + ssl_certificate directives.

upstream backend {
    server backend:8000;
    keepalive 32;
}

# Rate limiting zone — 10 req/s per IP (burst 20)
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;

server {
    listen 80;
    server_name your-domain.com;

    # Redirect HTTP → HTTPS in production
    # return 301 https://$host$request_uri;

    # Security headers
    add_header X-Content-Type-Options  "nosniff"         always;
    add_header X-Frame-Options         "DENY"            always;
    add_header X-XSS-Protection        "1; mode=block"   always;
    add_header Referrer-Policy         "strict-origin"   always;

    client_max_body_size 6M;

    # API — proxy to FastAPI backend
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        proxy_pass         http://backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
        proxy_read_timeout 120s;
    }

    # Health check — no rate limit
    location /health {
        proxy_pass http://backend;
        proxy_set_header Host $host;
    }

    # Frontend static files
    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
    }
}

# HTTPS server block (uncomment and configure after Certbot)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     include /etc/nginx/snippets/ssl-params.conf;
#     ... (same location blocks as above)
# }
